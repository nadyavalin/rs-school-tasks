import { createInput } from "./elements";

export const inputChooseCarModel = createInput(
  "text",
  "model",
  "input-car-model",
  "Choose the model",
);

export const inputChooseCarColor = createInput(
  "color",
  "color",
  "input-color",
  "",
);

export const inputUpdateCarModel = createInput(
  "text",
  "model",
  "input-car-model",
  "Choose the model",
);
export const inputUpdateCarColor = createInput(
  "color",
  "color",
  "input-color",
  "",
);
